<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bread_in_deajeon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bread_in_Deajeon">
        <activity
            android:name=".login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".bakery_list" android:label="bakery list"></activity>
        <activity android:name=".MainActivity" android:label="MainActivity"></activity>
        <activity android:name=".signUp" android:label="signup"></activity>
        <activity android:name=".sungsim" android:label="sungsim"></activity>
        <activity android:name=".harehare" android:label="harehare"></activity>
        <activity android:name=".mong" android:label="mong"></activity>
        <activity android:name=".bovolo" android:label="bovolo"></activity>
        <activity android:name=".colmare" android:label="colmare"></activity>
        <activity android:name=".request" android:label="request"></activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

</manifest>